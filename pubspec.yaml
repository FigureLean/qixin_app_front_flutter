name: qixin_app
description: "栖心App的Flutter前端"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.5.1
  bruno: ^3.4.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  pretty_dio_logger: ^1.3.1


flutter:
  uses-material-design: true
  assets:
    - sources/images/
